#pragma once
#include "../lib.h"
#include "Sorting.h"

int SearchElement(vector<int> ,int);

int DifferentValuesInMultiplicationTable(vector<vector<int>>, int, int);

vector<int> TwoSum(vector<int>&, int);

int mySqrt(int);

bool isValid(string);

int MaxProfit(vector<int>&);

bool isPalindrome(string);

bool isAnagram(string, string); 

int BinarySearch(vector<int>&, int );

int maxSubArray(vector<int>&);

// int RemoveElement(vector<int>&,int);

int climbStairs(int);

int lengthOfLastWord(string);

int missingNumber(vector<int>&);

int singleNumber(vector<int>&);

vector<vector<int>> insert(vector<vector<int>>&, vector<int>&);

int coinChange(vector<int>&, int);

bool isPalindrome(int);

int majorityElement(vector<int>&);



